export const environment = {
  production: false,
  // Proxied by proxy.conf.json during `ng serve` (see README) so the browser
  // never needs the backend's real origin during development.
  apiBaseUrl: '/api/v1',
};
